import { Text, View } from "react-native";

const Goals = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Goals</Text>
    </View>
  );
};

export default Goals;
